Run the full test suite and report results.

Execute: `pytest -v`

If any tests fail:
1. Read the failing test file and the corresponding source file
2. Identify the root cause
3. Report what failed and why, with a suggested fix
4. Do NOT modify code unless explicitly asked

If all tests pass, report the count and confirm green.